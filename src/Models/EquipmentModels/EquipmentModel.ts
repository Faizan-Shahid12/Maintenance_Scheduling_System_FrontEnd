export interface Equipment {
  equipmentId: number;
  name: string;
  type: string;
  location: string;
  serialNumber: string;
  model: string;
  isArchived: boolean;
  workShopName: string;
  workShopLocation: string;
}